# kardboard pipeline test

Throwaway file. It exists only so a kardboard Session can test pushing, opening a pull request,
the `verify` check, and the Cloudflare Pages preview. Close the pull request; do not merge it.
